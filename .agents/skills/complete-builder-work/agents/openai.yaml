interface:
  display_name: "Complete Builder Work"
  short_description: "Deliver Builder work or verify final closure"
  default_prompt: "Use $complete-builder-work. Run implementation plan review, development, verification, PR creation where required, CI gates, merge, and issue closure. Append progress and closure to the existing project memory; use only plan/report/project-memory documents. Apply $write-jane-street-style-code before code changes, preserve publication checkpoints and closure readback."
policy:
  allow_implicit_invocation: true
