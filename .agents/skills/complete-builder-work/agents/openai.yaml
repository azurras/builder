interface:
  display_name: "Complete Builder Work"
  short_description: "Deliver Builder work or verify final closure"
  default_prompt: "Use $complete-builder-work. Choose full delivery or closure-only from the request. Full delivery includes spec review, implementation plan review, development, PR creation where required, CI gates, merge, authorized deployment, committed continuity, and issue closure with readback. Apply $write-jane-street-style-code before code changes. Preserve phase checkpoints and runtime evidence rules."
policy:
  allow_implicit_invocation: true
