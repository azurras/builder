interface:
  display_name: "Save Test Report"
  short_description: "Save local app test reports"
  default_prompt: "Use $save-test-report to save a dated Markdown test report under docs/test-reports with local app details, data sent, responses received, pass/fail results, and evidence."
