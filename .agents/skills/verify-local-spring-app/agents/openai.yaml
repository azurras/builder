interface:
  display_name: "Verify Local Spring App"
  short_description: "Verify isolated Spring app runtime"
  default_prompt: "Use $verify-local-spring-app to verify effective test isolation, exercise the candidate and clean it up; deploy only within existing authority."
