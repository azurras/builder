interface:
  display_name: "Record Runtime Verification"
  short_description: "Save or validate local application evidence"
  default_prompt: "Use $record-runtime-verification. Save or validate real runtime inputs, outputs and pass/fail evidence. Keep reports separate and link them from the project memory. Use the skill reference template; no templates folder under docs."
policy:
  allow_implicit_invocation: true
