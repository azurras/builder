interface:
  display_name: "Write Jane Street-Style Code"
  short_description: "Apply the rigorous house coding standard"
  default_prompt: "Use $write-jane-street-style-code before editing code: load the routed references, write the five-field Before-Edit Brief, test first, and apply the blocker/warning review rubric."

policy:
  allow_implicit_invocation: true
