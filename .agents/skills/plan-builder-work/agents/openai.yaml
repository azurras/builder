interface:
  display_name: "Plan Builder Work"
  short_description: "Create specs and plans or review existing plans"
  default_prompt: "Use $plan-builder-work. Choose spec, plan, review, validate, or optional decision mode. Use only the requested scope. Review/validate are read-only. Apply $write-jane-street-style-code and task-specific Before-Edit Brief contracts to code-changing plans. Publish spec and plan checkpoints separately."
policy:
  allow_implicit_invocation: true
