interface:
  display_name: "Save Session Memory"
  short_description: "Append progress to stable project memory"
  default_prompt: "Use $save-session-memory. Append to the existing project file using required --project; dates and titles label entries. Preserve prior entries and link primary plan/report evidence. Record progress, reviews, decisions, repository context and closure in that file. Finalize authorized updates and publish continuity before external closure."
