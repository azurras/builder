interface:
  display_name: "Save Session Memory"
  short_description: "Record work in separate dated session files"
  default_prompt: "Use $save-session-memory to record work and events in the file for the actual work date and project, preserving earlier entries."
