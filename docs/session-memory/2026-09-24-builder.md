# 2026-09-24 - builder Session Memory

Work, decisions, events, and evidence for this date.

## 2026-09-24 18:34 - Mongo URI validator and production recovery

## 2026-09-24 18:34 Central Daylight Time - merged Mongo URI validator; production remains stopped

Continued the approved site bug audit in worktree `A:\Projects\christopherbell.dev-worktrees\mongodb-uri-database-required-20260924`. PR #1439 (`bbef48a8e428a64f5efafbee198c00dda3ea8d80`) passed all pre-merge CI/CodeQL/dependency checks and merged at 23:29 UTC as `fb10948cc4d0b3b2d5f739789b8a810713456301`. The guard now rejects Mongo connection strings without a named database, preventing the original Spring `Database name must not be empty` error from surfacing only during later startup.

Focused initializer regressions and `:website:check` passed. For runtime proof, cloned the checksummed restored backup fixture into a fresh disposable directory, started MongoDB only on loopback port 27019 and the `test` profile candidate on port 18081, and kept production MongoDB PID 5388 untouched. Candidate readiness returned HTTP 200/`UP`, and homepage returned HTTP 200 with title `CB | Home`. Stopped the candidate and isolated MongoDB afterward; both candidate ports closed. Detailed evidence is in `docs/test-reports/2026-09-24-mongo-uri-validation-and-production-runtime.md`.

After merge, the SYSTEM poller refreshed tools to `fb10948`, attempted the new main SHA, and recorded `DEPLOYMENT_FAILED`/`SERVICE_STOPPED`. Read-only `prod.cmd auto-status` reports `SERVICE_UNHEALTHY`, active SHA `4b552a63a08c9333bdaa0d7827b23eb811920f37`, failed/attempted SHA `fb10948cc4d0b3b2d5f739789b8a810713456301`, and sanitized poller status `UNKNOWN/ACCESS_DENIED`. Public readiness remains HTTP 502. Merge-triggered CI run 36072990717 was still in progress at this entry time.

The guarded Task 41 recovery script remains prepared and has not run. This task runner rejected a fresh elevated launch before Windows displayed UAC; no protected production configuration, marker, ACL, Mongo data, service, or listener was changed. The user's approval for the guarded repair is already on record; do not bypass the runner restriction. Resume by executing the prepared script through an authorized elevated Windows prompt, then inspect its result and verify local/public readiness, service state, active SHA, and restored recovery policy. Keep Task 41 and the site bug audit open until production acceptance is actually observed.


## 2026-09-24 18:35 Central Daylight Time - Correct project memory placement

## 2026-09-24 Correction - project record placement

Correction to the 18:34 entry above: the Mongo URI validator, site outage, PR #1439 and production recovery are `christopherbell-dev` work, not Builder platform work. The original entry is retained for audit history; the project-specific record is in `docs/session-memory/2026-09-24-christopherbell-dev.md`.
