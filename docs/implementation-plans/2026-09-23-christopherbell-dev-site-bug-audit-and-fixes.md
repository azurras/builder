# christopherbell.dev site bug audit and fixes

## Plan Format
task-contract-v1

## Document Status
in-progress

## Objective
Find and fix reproducible bugs in the current christopherbell.dev site, verify each correction, and complete the authorized delivery and production acceptance workflow.

## Goals
- Surface real defects through the repository's existing checks, focused code-path review, and safe live-site smoke checks.
- Fix as many confirmed defects as can be completed and safely delivered in this workstream.
- Keep production data and user accounts unchanged during discovery and local verification.

## Inputs
- User authorizes broad bug discovery, fixes, verification, and necessary delivery actions.
- Live GitHub issue inventory is empty; open PRs #1387 and #1388 are dependency updates.
- Live homepage and all sitemap non-restaurant routes returned HTTP 200 on 2026-09-23.
- Live browser homepage, Void, WFL ranking and Raising Cane's tracker had no console errors; tracker loaded after its API request completed.
- WFL public freshness API reports `lastRefreshedOn=2026-08-02T22:44:50.963Z`, `current=false`, and a 45-day freshness window.
- Cane's public history API on 2026-09-23 reported only 9 snapshots from 2026-06-01 through 2026-09-07; no snapshots existed for the scheduled 2026-09-14 or 2026-09-21 weeks. The latest snapshot was complete (50/50 metro entries, collected 2026-09-07T11:00:16.849Z). The Monday 06:00 America/Chicago schedule's root cause for the missing weeks remains unconfirmed.
- The production WFL scheduler is configured by default for the 15th at 03:00 America/Chicago; the import startup catch-up compares only the prior month's successful completion month.
- Protected application logs are unreadable to this account; do not change their ACLs. The import's exact production failure cause remains unconfirmed.
- `:website:check` passed on `origin/main` SHA `feb3f78ae24cf4b22c4035b25068fb78a1b68d5c` after setting a per-process short JDK Unix-domain socket temp path. No environment or `.env` overrides were present in the isolated worktree. The full check used mocked/unit boundaries; external PostgreSQL contract tests were skipped because the local test role has no configured password.
- The authoritative checkout has unrelated local changes; implementation uses `A:\Projects\christopherbell.dev-worktrees\site-bug-audit-20260923`.

## Branch
`codex/site-bug-audit-20260923` from refreshed `origin/main` `feb3f78ae24cf4b22c4035b25068fb78a1b68d5c` delivered PR #1398. Cane's follow-up was developed in `codex/site-bug-audit-round2-20260923`, based on merged SHA `f1db692c7b5dff1de7ce4490c07908935ee8f8b0`, and delivered PR #1399. Deployment corrections #1400-#1406 are merged through `e352dbaef7bd2da4b10e591961230d5182a1d897`. Task 8 was developed from that `origin/main` SHA on `codex/account-profile-viewer-read-failure-20260924` and merged as PR #1407 at `0028642e712f1e0e1dedd41e0e75bbbc79960dd7`. Task 9 is planned from refreshed `origin/main` `0028642e712f1e0e1dedd41e0e75bbbc79960dd7` on `codex/auto-deploy-future-status-20260924`. Preserve the dirty authoritative checkout.

## Non-Goals
- No destructive or mutating production actions during discovery or local verification.
- No unrelated feature redesign, dependency-only changes, or opportunistic cleanup.
- Do not claim that an audit exhaustively proves the absence of all defects or that the WFL catch-up gap is the only cause of stale production data.

## Assumptions
- Read-only public production smoke checks and supported deployment for verified fixes are authorized.
- Tests must use isolated PostgreSQL `test` data; no test or candidate may connect to production data.
- Only independently reproducible or source-confirmed correctness failures are eligible for fixes.
- On deployment, the existing startup catch-up mechanism is expected to perform its configured WFL import when its latest scheduled run is overdue.

## Open Questions
- Protected production import status/log details remain unavailable. Report this as a production-cause limitation; do not bypass protected ACLs.

## Task Breakdown

### Task 1 - Establish audit evidence and identify confirmed defects
- Dependencies: None.
- Files: `AGENTS.md`, `README.md`, `website/src/main/java/dev/christopherbell/view/README.md`, `website/src/main/java/dev/christopherbell/configuration/README.md`, `website/src/main/resources/static/js/README.md`, application profiles, public templates/routes, WFL import workflow and tests.
- Symbols: Test/runtime profile and persistence settings, page mappings/security, public freshness route, browser assets, WFL monthly cron and startup catch-up, native `:website:check` tasks.
- Inspection: Clean isolated worktree at refreshed `origin/main` SHA above. Verified PostgreSQL test profile resolves to database `test` and role `christopherbell_test`; no effective password is available. Live public routes and current WFL freshness API were read without mutation. Protected production log access returned ACL denial. Read the WFL import package README and traced `runMissedMonthlyOpenStreetMapImport`.
- Behavior: Establish baseline checks and safely observable public routes; distinguish verified bugs from expected authorization, unavailable integrations and status indicators.
- Invariants: No production writes during audit; no production database connections from tests/candidate; preserve dirty authoritative checkout; do not infer causation from freshness status alone.
- Boundary/API: Existing routes, documented Gradle tasks, and non-mutating HTTP GET requests only.
- Effects and failures: Reads only; redact credentials and personal data; protected logs remain untouched; stop database-backed execution if the target is not isolated.
- Tests and evidence: Verified test profile config; `:website:check` completed successfully on baseline; all sitemap routes excluding 7,340 restaurant-detail URLs returned 200, and one restaurant detail plus `/wfl/top-liked` returned 200. Browser checked homepage, Void and tracker state with no console errors. Live freshness endpoint returned stale status as recorded above.
- Verification: Confirmed the monthly schedule and catch-up comparison in source. Startup catch-up fails to retry after the current month's scheduled occurrence when the prior successful completion predates that occurrence; add a deterministic regression test in Task 2. A targeted scan found no other monthly prior-completion-month check. It did surface a separate stale Cane's weekly history symptom; its startup recovery is specified in Task 3 while the reason for missed scheduled executions remains unconfirmed.

### Task 2 - Retry overdue monthly imports on application startup
- Dependencies: Task 1 audit evidence.
- Files: `website/src/main/java/dev/christopherbell/whatsforlunch/restaurant/importing/RestaurantImportWorkflowService.java`, `website/src/test/java/dev/christopherbell/whatsforlunch/restaurant/importing/RestaurantImportWorkflowServiceTest.java`, and `website/src/main/java/dev/christopherbell/whatsforlunch/restaurant/README.md`.
- Symbols: `runMissedMonthlyOpenStreetMapImport`, the monthly cron/zone properties, import state `lastCompletedOn`, and the `OpenStreetMap Import` scheduling description.
- Inspection: Read production scheduler and durable-state updates, the package README, WFL configuration, and existing workflow unit tests. The service records `lastCompletedOn` for successful imports, but startup catch-up compares only `lastCompletedMonth` to the previous calendar month. The live site freshness confirms data has remained unchanged since 2026-08-02, but protected logs prevent establishing the sole production cause.
- Required skill: `write-jane-street-style-code` before code changes.
- Behavior: After the latest configured monthly cron occurrence is due, startup retries when the latest successful completion predates that occurrence, even if an earlier success exists in the same month. Before the current month's scheduled occurrence, a successful run after the previous occurrence suppresses duplicate catch-up. Keep first-run catch-up behavior.
- Invariants: Preserve scheduled cron/zone behavior, the durable import lease and ownership checks, existing failure recording, deploy-smoke suppression, and admin API contracts. Disabled monthly imports remain disabled. No test/candidate connects to production data.
- Boundary/API: No endpoint or persistence schema changes. Use existing import state and cron configuration only.
- Effects and failures: Startup catch-up may fetch and apply configured OSM data through the existing lease-protected workflow. Preserve current logged failure behavior and never bypass the lease. Unit tests mock remote/import effects.
- Tests and evidence: Add regression cases for startup after an overdue monthly cron and startup before the next due occurrence; verify expected lease/import calls. Run focused `RestaurantImportWorkflowServiceTest`, full `:website:check`, then candidate runtime on an isolated port against a temporary PostgreSQL database named `test` and `deploy-smoke` profile so no scheduled external jobs run during smoke verification.
- Verification: The failing test reproduced the missed catch-up before implementation and the focused test passes after the fix. The full native `:website:check` passed, `git diff --check` was clean, and the packaged candidate passed readiness, homepage, ranking, and freshness requests against the disposable `test` database. PR #1398 was merged as `f1db692c7b5dff1de7ce4490c07908935ee8f8b0`; CodeQL, dependency review, and Java, JavaScript, macOS, Ubuntu, and Windows checks passed. The production release is not yet verified: on 2026-09-23 at 22:20, 22:23, and 22:24 UTC, the website service remained Running on PID 14424 and public WFL freshness remained `2026-08-02T22:44:50.963Z`. The unprivileged shell could not read protected deployment state or start the SYSTEM task, and the platform rejected a `RunAs` dry-run launch. Do not weaken ACLs or bypass the supported deployment path; resume with an authorized elevated deployment session, then verify release SHA, readiness, routes, and WFL import freshness.

### Task 3 - Catch up an overdue Cane's weekly collection on startup
- Dependencies: Task 1 live freshness and scheduler audit.
- Files: `website/src/main/java/dev/christopherbell/canesboxtracker/CanesBoxTrackerService.java`, `website/src/main/java/dev/christopherbell/canesboxtracker/model/CanesBoxTrackerProperties.java`, `website/src/test/java/dev/christopherbell/canesboxtracker/CanesBoxTrackerServiceTest.java`, and `website/src/main/java/dev/christopherbell/canesboxtracker/README.md`.
- Symbols: `collectCurrentWeek`, current week selection, `CanesBoxPriceSnapshot.collectedOn`, the weekly cron and zone, and the durable `canes-box-price-collection` lease.
- Inspection: Public history currently has a complete Sep 7 snapshot (50 metros) but no Sep 14 or Sep 21 snapshots despite a Monday 06:00 America/Chicago cron. `fetchMetroPrice` converts per-metro exceptions into failed datapoints and `collectWeek` still saves a full snapshot; absent week records therefore show that no complete scheduled workflow was persisted, but do not identify why. The current service has no startup catch-up.
- Required skills: `write-jane-street-style-code` and focused regression tests before implementation.
- Behavior: At application readiness, when the feature is enabled and `deploy-smoke` is not active, inspect recent snapshots and retry the current week's collection through the existing lease-protected method if a configured weekly cron occurrence after the latest complete metro snapshot is due. A current-week full snapshot suppresses duplicate startup collection. A manually entered partial current-week snapshot does not hide an overdue scheduled collection. With no history, perform the existing first-data catch-up behavior.
- Invariants: Keep the Monday cron and configured time zone, current-week key semantics, provider failure capture, coordinator/lease ownership, and admin APIs. Do not start collection in `deploy-smoke` or when disabled. No schema/API changes; no local request reaches production data or the external Cane's service during unit tests.
- Boundary/API: Existing weekly history repository, schedule configuration, and coordinator only.
- Effects and failures: An overdue startup collection can make the existing configured provider calls for the metro set and persist the current week's snapshot. Lease contention skips work. Existing per-metro failures remain recorded. No change to cutover or local candidate scheduling behavior.
- Tests and evidence: Add regression coverage for an overdue prior complete snapshot, no retry before the next scheduled occurrence, duplicate suppression when this week's complete snapshot already exists, and incomplete current-week manual data. Run focused `CanesBoxTrackerServiceTest`, full `:website:check`, then the packaged candidate on an isolated port and disposable database; startup network effects remain disabled in `deploy-smoke`.
- Verification: Live API read at 2026-09-23 22:32:23 UTC confirmed latest week `2026-09-07`, collected at `2026-09-07T11:00:16.849Z`, with 50/50 successful metro prices and no week entries for Sep 14 or Sep 21. The API returned HTTP 200. Regression tests failed before the catch-up logic and passed afterward; focused `CanesBoxTrackerServiceTest` and full `:website:check` passed. The packaged candidate ran with `test,deploy-smoke` on port 8082 against a disposable PostgreSQL 18 `test` cluster; all seven health/page/API GETs returned HTTP 200 and both candidate and cluster were stopped. Report: [Cane weekly catch-up runtime verification](../test-reports/2026-09-23-2026-09-23-christopherbell-dev-cane-weekly-catch-up-runtime-verification.md). PR #1399 passed dependency review, CodeQL, Java and JavaScript analysis, and macOS, Ubuntu and Windows builds; it was squash-merged at 2026-09-23 23:06:42 UTC as `5f89e5c286e9c96b1d45dcfbd88bc036cc39c34c`. Post-merge CI Build passed on Windows (9m03s), macOS (4m08s), and Ubuntu (3m44s). Latest public read at approximately 2026-09-23 23:16 CDT still shows the Sep 7 Cane's snapshot and stale WFL freshness timestamp `2026-08-02T22:44:50.963Z`; production remains on listener PID 14424. No protected state or logs were read. Deployment and production acceptance remain blocked because the local token cannot read protected deployment state or start the SYSTEM task, and the platform rejected an elevation request. Do not weaken ACLs or bypass the supported deployment path; resume from an authorized elevated deployment session.

### Task 4 - Make the Windows SYSTEM deployment test run pass
- Dependencies: PR #1399 merge; production deployment acceptance of its runtime build.
- Files: `website/src/test/java/dev/christopherbell/configuration/persistence/migration/FinalizeEvidenceLoaderTest.java`.
- Symbols: `productionAuthorityRootIsFixedAndSelfMintedOwnerOnlyFilesAreRejected`, `AclFileAttributeView`, and production write-principal validation.
- Inspection: After the user approved elevation, read the protected auto-deploy state and confirmed `ChristopherBellAutoDeploy` had been disabled since 2026-09-05. The supported `prod.cmd auto-install -WhatIf` preflight passed; running documented `prod.cmd auto-install` from the worktree whose Git tree matches merged `origin/main` restored the SYSTEM poller. It detected remote SHA `5f89e5c286e9c96b1d45dcfbd88bc036cc39c34c`, built 2,282 tests, and failed one after 6m45s. The Gradle daemon report names `FinalizeEvidenceLoaderTest.productionAuthorityRootIsFixedAndSelfMintedOwnerOnlyFilesAreRejected` with `UserPrincipalNotFoundException` at the current-user lookup line. A first alias qualification fix was merged as PR #1400, but the subsequent actual SYSTEM build at `de7904065c202b839ad9d4e3be477c6fd287127e` failed the same test and exception class. The auto-deploy cleanup removed the failed release worktree and test XML; the live service was never stopped. The poller remains enabled and records the SHA as failed.
- Required skills: `write-jane-street-style-code` and focused regression verification.
- Behavior: Remove dependence on the SYSTEM process account name from this fixture. Make the self-minted Windows file carry an explicit untrusted `Everyone` write ACE so production write-principal validation must reject it; preserve the fixed production-root assertion.
- Invariants: Test-only change; preserve production ACL policy and evidence-authority checks; never leave owner-only ACLs or test artifacts in ProgramData; keep production service and listener untouched until the fully guarded deployment reaches cutover.
- Boundary/API: Local Windows test fixture principal lookup only; no production endpoint, schema or runtime authorization changes.
- Effects and failures: Only the isolated Gradle test fixture is affected. The existing automatic deploy uses the protected Gradle home; do not manually delete or modify its caches.
- Tests and evidence: Both actual SYSTEM test runs provide the red case for the original username-dependent fixture: merged `5f89e5c` failed with one test failure after 6m45s; PR #1400 SHA `de790406` failed the same test after 5m53s (2,283 tests, one failed, 300 skipped). Revised fixture adds an explicit untrusted `Everyone` write ACE. All eight focused tests pass normally and with JVM `user.name=SYSTEM`; full native Windows `:website:check` passed in 5m27s. PR #1401 merged at `78147bcb6fe820fb1982863fcea1b8ad390def04` after Ubuntu, macOS, Windows, CodeQL and dependency-review checks passed. The SYSTEM build of this SHA passed (2,283 tests, 300 skipped) and produced an executable release directory; candidate startup then failed before port 8081 bound.
- Verification: In progress. PR #1401 merged at 2026-09-24 01:17:10 UTC. At 01:23:51 UTC the enabled poller recorded `78147bcb6fe820fb1982863fcea1b8ad390def04` failed with `Cannot find a process with the process identifier 9676`; the candidate listener was absent and the website service remained Running on port 8080/PID 14424. Public readiness remains UP; WFL freshness is still `2026-08-02T22:44:50.963Z`, Cane's history has nine weeks through Sep 7. The protected Gradle log confirms BUILD SUCCESSFUL in 5m57s. Candidate output was not captured in the app log, which had not updated during this attempt; exact application startup cause is unknown. Task 5 must improve diagnostics before another guarded release attempt.

### Task 5 - Preserve candidate startup diagnostics when the process exits
- Dependencies: Task 4 fixture fix merged; actual SYSTEM build now passes but candidate validation cannot complete.
- Files: `ops/production/windows/modules/Production.Deploy.psm1`, `ops/production/windows/tests/Production.Deploy.Tests.ps1`, and the deployment module's existing production log boundary.
- Symbols: `Start-ProductionJar`, `Wait-ProductionCandidateOwnedListener`, and `Test-CandidateRelease`.
- Inspection: The enabled SYSTEM poller built `78147bcb6fe820fb1982863fcea1b8ad390def04`; the Gradle daemon reports `BUILD SUCCESSFUL in 5m 57s`. Release metadata was written, but candidate PID 9676 exited before binding configured port 8081. The poller recorded generic `Cannot find a process with the process identifier 9676`. The protected shared application log had not updated during the attempt, and guarded cleanup removed the temporary worktree/database. Production remained healthy on PID 14424. Candidate launch did not redirect its standard output or error. Follow-up inspection also confirmed the SYSTEM task executes a protected copied tool bundle, which does not refresh when source merges; the supported `auto-install` command must publish module updates to that bundle.
- Required skills: `write-jane-street-style-code` and focused Windows Pester regression verification.
- Behavior: Capture bounded candidate standard output/error under the protected production logs directory and surface a useful sanitized exit diagnostic when the spawned process exits before binding. Preserve the candidate's exact process identity check and existing guarded cutover.
- Invariants: Do not log environment values, secrets, request data, or database contents; keep candidate logs under the protected logs root with a bounded size; never stop or replace the live service on candidate failure; preserve exact candidate database cleanup.
- Boundary/API: Internal Windows deployment process and logs only; no application endpoint or schema changes.
- Effects and failures: Candidate process log creation only during an already authorized deploy validation. Candidate failure must retain its causal startup evidence while leaving production listener/service unchanged.
- Tests and evidence: Added a real child-process regression that writes 70,000 bytes and verifies the captured output remains at or below 65,536 bytes. Added a candidate-exit regression that verifies PID/exit-code/log-path reporting, protected-log location, and that the candidate database name is not surfaced. `Production.Deploy.Tests.ps1` passed 90/90. Native Windows `:website:check` passed in 3m32s; Windows PowerShell 5.1 imported the deployment module successfully. PR #1402 passed Ubuntu, macOS, Windows, Java/JavaScript/Actions analysis, CodeQL, and dependency review, then merged as `664ff133d365f300fa0c96e2f70fa357576d8687`.
- Verification: PR #1402's protected-tool refresh succeeded through `prod.cmd auto-install`, and its installed module hash matched the merged source. Once the 900-second failure backoff expired, the updated poller attempt at 2026-09-24 02:26:01 UTC stopped before candidate startup because the shared `logs` directory does not satisfy `Assert-ProtectedProductionPath` (`Protected production path must disable ACL inheritance`). No shared ACL was changed. PR #1403 (`bcb883b89813ad20fb977e1ff89572464381717a`) adds a dedicated `logs\candidate` child with the exact protected SYSTEM/Administrators ACL; its dependency review, CodeQL, language analyses, and Ubuntu, macOS, and Windows build/test checks all passed. PR #1404 (`9a9e39b28517436ca9beb361bd9c959a48baa067`) subsequently fixed install-lock ordering and all required checks passed. Current unprivileged readback at 2026-09-23 23:40 CDT confirms public readiness HTTP 200/UP, but the WFL freshness marker is still `2026-08-02T22:44:50.963Z`, Cane's history remains nine weeks through 2026-09-07, and standard-user auto-status says the #1405 status store is not initialized. No fresh candidate attempt can be attributed to the latest tools without protected task state/log access. Production acceptance remains in progress until the merged tools are bootstrapped, the updated bundle deploys, and post-deployment data recovery is verified.

### Task 6 - Make automatic deployment status readable and failures observable
- Dependencies: Task 5 diagnostics and PR #1403's protected candidate log boundary; PR #1404's install-lock ordering fix is merged as `9a9e39b28517436ca9beb361bd9c959a48baa067`.
- Files: `ops/production/windows/modules/Production.AutoDeploy.psm1`, `ops/production/windows/prod.ps1`, `ops/production/windows/tests/Production.AutoDeploy.Tests.ps1`, `README.md`, and the current deployment-plan evidence.
- Symbols: `Invoke-AutoDeployOnce`, `Start-AutoDeployLoop`, `Get-AutoDeployStatus`, and automatic task registration/status publication.
- Inspection: The scheduled SYSTEM process wakes about once per minute after PR #1404 merged, but only listener 8080 remains active and the deployed service/data are unchanged. Standard-user `prod.cmd auto-status` fails before reading state because protected `config\deploy.json` denies access. Source inspection confirms deployment failures are caught, written to protected state, then swallowed; Windows Task Scheduler can therefore record a successful one-shot even when deployment failed. `C:\ProgramData` grants ordinary Users file creation but not subdirectory creation; any new readable status directory must be created from a secured unpredictable sibling stage and atomically published, rejecting a pre-created destination or reparse point. Current `auto-status` also emits the same PowerShell unapproved-verb discovery warning three times before status output because `Production.Deploy` is imported directly and through two dependent modules.
- Required skill: `write-jane-street-style-code` before code changes.
- Behavior: A standard user can read a concise `auto-status` report without protected configuration or elevated access. The report gives freshness, poll/deployment phase, main/active/attempted/successful/failed SHAs, retry time, and a safe fixed message. Failed deployment attempts remain failed task results and publish sanitized status. Suppress only the known unapproved-verb discovery warning at those import sites; leave other warnings observable.
- Invariants: Configuration, deployment controls, logs, and raw exception details remain protected. The public status store is a fixed sibling of the protected root, reparse-free, atomically updated, readable by standard users, writable only by SYSTEM/Administrators, and limited to an allowlist of nonsecret fields. Status publishing must not permit a standard user to influence control flow or deployment inputs.
- Boundary/API: `prod.cmd auto-status` becomes a read-only, non-elevated CLI operation backed only by a versioned JSON status document; no web/API contract changes.
- Effects and failures: `auto-install` securely creates or validates the status store. The scheduled invocation publishes checking, up-to-date, backing-off, deploying, succeeded, blocked, and failed outcomes. Invalid paths/ACLs fail closed before deployment; publisher errors must not mask the original deployment error.
- Tests and evidence: Added Pester cases for status path ACL, standard-user-readable status without config access, stale/malformed status handling, sanitized error output, surfaced deployment failure, marker failure classification, exact trusted tree staging, manifest hash verification, partial-stage cleanup, task-action preservation, lock release ordering, and a CLI warning-stream regression that failed before the fix because `Production.Deploy` emitted the same unapproved-verb warning three times. Adding `-DisableNameChecking` only to the three direct/nested import sites made the test pass while preserving all other warnings. `Production.AutoDeploy.Tests.ps1` passed 22/22; `Production.Operations.Tests.ps1` passed 91/91. Windows PowerShell 5.1 CLI help and module import passed. Installed Pester 3.4 cannot run the repository's Pester 5 assertion syntax. Native `:website:check` was attempted twice for the earlier deployer implementation, including with a private Gradle home; both attempts stopped before task configuration because the JDK could not establish its local loopback connection (`java.io.IOException: Unable to establish loopback connection`). The actual documentation change is `docs/operations/windows-production.md`.
- Verification: Local standard-user `prod.cmd auto-status` now emits a concise result without the repeated import warnings and returns `available=False`, `freshness=UNAVAILABLE`, `reason=STORE_NOT_INITIALIZED`, without prompting or reading protected deployment configuration. PR #1406 passed dependency review, CodeQL, Java/Kotlin and JavaScript/TypeScript analysis, and Windows, macOS, and Ubuntu build/test; it squash-merged on 2026-09-24 CDT as `e352dbaef7bd2da4b10e591961230d5182a1d897`, and merge-triggered CodeQL and all three platform builds passed. The installed SYSTEM task has not yet been refreshed, so status publication and version switching are not live-verified. The first bootstrap still requires the documented elevated `auto-install`; subsequent tool refreshes use the existing SYSTEM task without prompts. Do not claim production acceptance until that bootstrap, task refresh, current-release deployment, and public data freshness checks complete.

### Task 7 - Make the installed SYSTEM poller refresh trusted deploy tools
- Dependencies: Task 6 standard-user status and PR #1404's release-lock-before-task-start ordering.
- Files: `ops/production/windows/modules/Production.AutoDeploy.psm1`, `ops/production/windows/tests/Production.AutoDeploy.Tests.ps1`, and `docs/operations/windows-production.md`.
- Symbols: `Invoke-AutoDeployOnce`, `Update-ProductionAutoDeployToolsUnderHeldLock`, `Install-AutoDeployTask`, protected release worktrees, and the scheduled task action.
- Inspection: The task runs from the protected copied tool bundle; code inspection shows only administrator-run `auto-install` refreshes it. The production deployer already fetches exact origin/main SHAs and creates protected detached worktrees under `worktrees\\<full-sha>`. The current task wakes every minute but the checked-in change to its tool installer is not automatically reflected in that copied bundle.
- Required skill: `write-jane-street-style-code` before code changes.
- Behavior: Once the existing SYSTEM task is installed, routine merges that change deployer tools require no administrator prompt. On a trusted main-tree change, the task stages an immutable, versioned protected tool bundle from that exact commit, verifies the Git tree identity and every copied file hash, registers the new task action while holding `deploy.lock`, releases the lock, and lets the next invocation run the new version. Versions are keyed by the deploy-tools tree hash so ordinary site commits do not create duplicate bundles. It does not deploy from a dirty developer checkout or overwrite the currently executing bundle.
- Invariants: Only the configured trusted Git remote/ref and full fetched commit SHA select code. A complete staged bundle and exact ACL/reparse validation precede task registration. Invalid/incomplete stages preserve the currently registered task and active version. Public status grants no write/delete rights and cannot influence task action arguments.
- Boundary/API: Existing `auto-install` remains the explicit initial provisioning/recovery path; ordinary deployer updates after that run through the SYSTEM task without user or elevated interaction.
- Effects and failures: The task may add a version directory and replace its own scheduled action while holding the production lock. It does not stop or mutate its executing files. Registration happens only after complete publication; the lock is released before the next scheduled invocation. Failure keeps the current action and reports safe refresh status.
- Tests and evidence: Pester covers exact-commit worktree validation, source/tree SHA recording, per-file hashes, immutable staging, partial-stage cleanup, ACL/reparse validation ordering, old-action preservation on stage failure, no task stop/start, and deployment lock release ordering. Focused Pester and operations suites passed; Windows PowerShell 5.1 module import passed. Record the failed native Gradle loopback attempt in the report rather than treating it as a product failure.
- Verification: PR #1405 passed dependency review, CodeQL, Java/Kotlin and JavaScript/TypeScript analysis, and Windows, macOS, and Ubuntu build/test jobs; the Windows rerun passed after a transient Pester Gallery lookup failure. The PR squash-merged on 2026-09-23 CDT as `9d4929af94a7be916365552fad84d67bb56c4590`; merge-triggered CodeQL and all three OS build/test jobs also passed. At 2026-09-23 23:40 CDT, standard-user `prod.cmd auto-status` still returned `STORE_NOT_INITIALIZED`; public readiness and homepage returned HTTP 200/UP, WFL freshness returned HTTP 200 with `lastRefreshedOn=2026-08-02T22:44:50.963Z` and `current=false`, and Cane's history returned HTTP 200 with nine snapshots through 2026-09-07. Confirm `auto-status` remains readable by a standard user across a tool-version switch. The live poller has not yet been bootstrapped with this code; one approved elevated `auto-install` is required before production verification. Record process/listener/readiness and public data evidence after bootstrap. Production acceptance still requires the deployed release SHA and recovered data freshness; do not weaken ACLs or claim unperformed checks.

### Task 8 - Preserve authenticated viewer state when public profile lookup fails
- Dependencies: Task 1 profile and API contract inspection; this issue was discovered during continued post-merge site audit.
- Files: `website/src/main/java/dev/christopherbell/account/profile/AccountProfileService.java`, `website/src/main/java/dev/christopherbell/account/README.md`, and `website/src/test/java/dev/christopherbell/account/AccountServiceTest.java`.
- Symbols: `getPublicProfile`, `getOptionalSelfAccount`, `getSelfEntity`, and the `followedByMe` / `self` response fields.
- Inspection: On `origin/main` `e352dbaef7bd2da4b10e591961230d5182a1d897`, read the profile service, repository interface and callers, `PermissionService.getSelf`, account README, account API contract documentation, and existing public-profile tests. `getSelfEntity` reads the authenticated account from `AccountRepository`; `getOptionalSelfAccount` catches every `Exception`, so a storage exception is converted to an empty viewer and the successful public-profile response incorrectly reports both viewer fields as false. Anonymous access instead raises `IllegalStateException` before repository access and must continue to work.
- Required skill: `write-jane-street-style-code` before code changes.
- Behavior: Anonymous public-profile reads continue to return public metadata with `self=false` and `followedByMe=false`. When authenticated viewer resolution reaches `AccountRepository` and storage fails, propagate that failure so the API does not return false viewer state as if the lookup succeeded.
- Invariants: Do not expose private account fields; do not change follow/unfollow or public route authorization; stale/missing viewer accounts retain existing anonymous-fallback behavior; production database state remains untouched.
- Boundary/API: Existing public account-profile response only; no field or route shape changes.
- Effects and failures: Read-only account lookup. Catch only expected absent-authentication / missing-viewer conditions; unexpected repository failures remain visible to the standard error handling and diagnostics.
- Tests and evidence: Add a regression proving repository failure after authenticated viewer resolution propagates, plus retain/extend anonymous-profile coverage to prove anonymous reads still succeed. Run the focused `AccountServiceTest`, then the applicable website check with test-database isolation verified.
- Verification: The new authenticated-storage regression failed before the fix and passed afterward; focused `AccountServiceTest` passed 48/48, including anonymous and missing-viewer fallbacks. Full `:website:check` passed (2,287 Java tests, 343 JavaScript tests, configured Pester suites; 302 database/provider Java tests and one real-ACL PowerShell case skipped by environment prerequisites). The packaged candidate passed liveness/readiness, homepage, local account creation, and anonymous profile retrieval on port 8082 using isolated PostgreSQL database `test`, role `christopherbell_test`, and schema `cbtest_profileviewer`; response was HTTP 200 with `self=false` and `followedByMe=false`. Candidate and disposable DB were stopped, production stayed unchanged, and `git diff --check` passed. PR #1407 and all required checks passed; merge-triggered CodeQL and Linux/macOS CI passed. The first post-merge Windows run had one `ConcurrentModificationException` in the unrelated `AsyncDispatcherSecurityIntegrationTest`; rerunning that failed job passed. Report: [Account profile viewer failure runtime verification](../test-reports/2026-09-24-christopherbell-dev-account-profile-viewer-failure-runtime-verification.md).

### Task 9 - Reject future-dated automatic deployment status
- Dependencies: Task 6 non-elevated status reader and status schema.
- Files: `ops/production/windows/modules/Production.AutoDeploy.psm1` and `ops/production/windows/tests/Production.AutoDeploy.Tests.ps1`.
- Symbols: `Get-AutoDeployStatus`, `New-UnavailableAutoDeployStatus`, and status freshness calculation.
- Inspection: On merged `origin/main` `0028642e712f1e0e1dedd41e0e75bbbc79960dd7`, the reader computes status age as `Now - updatedAt` and labels only ages over 180 seconds stale. A future timestamp therefore produces negative age and is reported `FRESH`, even though freshness cannot be trusted. TDD also exposed a timestamp parsing defect: `ConvertFrom-Json` converts the ISO `updatedAt` string to `[datetime]`, then `[string]` drops its UTC marker before `DateTimeOffset.Parse`, which interprets the wall time in the local zone and shifts freshness by the host UTC offset.
- Required skills: `write-jane-street-style-code` and test-driven development for the regression and fix.
- Behavior: Preserve the UTC instant when reading `updatedAt`; valid fresh/stale status records must remain correctly classified in any Windows time zone. A status timestamp later than the reader's UTC clock must be unavailable with an explicit safe `FUTURE_TIMESTAMP` reason; it must never be reported fresh.
- Invariants: Keep status reading non-elevated and independent from protected deployment configuration; do not change deployment control flow, status file permissions, or secret exposure.
- Boundary/API: Existing local `auto-status` JSON/console result only; no public web endpoint or status-file schema change.
- Effects and failures: Read-only validation. Invalid clock ordering fails closed for operator status without triggering or blocking a deployment.
- Tests and evidence: Add/retain Pester cases with fixed `Now` values for a valid stale timestamp, a valid fresh timestamp, and a timestamp later than `Now`; first observe the current incorrect classification, then require the correct freshness and `available=false`/`FUTURE_TIMESTAMP` for future data. Run `Production.AutoDeploy.Tests.ps1`, the related operations suite, and applicable deployment script checks.
- Verification: `Production.AutoDeploy.Tests.ps1`, `Production.Operations.Tests.ps1`, and `Production.Command.Tests.ps1` passed together under PowerShell 7/Pester 5.9 (135 passed, 0 failed). The same Windows PowerShell 5.1/Pester 5.9 run passed 133 tests and failed two tool-staging tests; both failures reproduce identically in a clean `origin/main` worktree, while both tests pass under PowerShell 7. Regression coverage uses fixed UTC `Now` values and confirms preserved timestamps, correct fresh/stale classification, and unavailable `FUTURE_TIMESTAMP` behavior. Non-elevated `prod.cmd auto-status` returned `STORE_NOT_INITIALIZED` without prompting; `prod.cmd help` passed. `git diff --check` passed. Production was not changed; its status store remains uninitialized pending the already-documented elevated bootstrap.

### Task 10 - Compare automatic-deploy backoff timestamps as instants
- Dependencies: Task 6 automatic deployment failure status and retry behavior.
- Files: `ops/production/windows/modules/Production.AutoDeploy.psm1` and `ops/production/windows/tests/Production.AutoDeploy.Tests.ps1`.
- Symbols: `Invoke-AutoDeployOnce`, persisted `failedAt`, and failed-SHA retry backoff.
- Inspection: The existing `backs off the same failed SHA` regression fails under Windows PowerShell 5.1. The persisted `failedAt` ISO-Z string is cast to `[datetime]`, becoming local-kind wall time; comparing it with the UTC-kind current time compares mismatched clock ticks and incorrectly allows a retry before the 900-second backoff expires. PowerShell 7 and Windows PowerShell 5.1 deserialize/represent the timestamp differently, so both paths need explicit coverage.
- Required skills: `write-jane-street-style-code` and test-driven development for the regression and fix.
- Behavior: Parse persisted failure timestamps without losing their offset/UTC instant and suppress a retry only while `Now` is earlier than `failedAt + configured backoff`. Preserve the existing backoff duration, failed-SHA matching, and retry-after-expiry behavior.
- Invariants: Keep persisted state schema and deployment selection unchanged; do not suppress new SHAs or retries after expiration; compare actual instants consistently across supported PowerShell versions.
- Boundary/API: Internal automatic-deploy retry timing only; no CLI, status schema, task registration, or web API changes.
- Effects and failures: A failed same-SHA deployment remains backed off for the configured interval on either PowerShell runtime. Invalid persisted timestamps continue through existing state validation/failure handling.
- Tests and evidence: First reproduce the existing same-failed-SHA test failure on Windows PowerShell 5.1, then require it to pass under both Windows PowerShell 5.1 and PowerShell 7. Run related automatic-deploy, operations, and command suites; compare any remaining Windows PowerShell 5.1-only staging-test failures with a clean `origin/main` baseline before attributing them to this change.
- Verification: The existing same-failed-SHA backoff regression failed under Windows PowerShell 5.1 before the timestamp parser change and passes afterward; the retry-after-expiry regression passes under Windows PowerShell 5.1 and PowerShell 7. PR #1408 merged as `8b0bcf0e1eca37645bd8a6df3fc603055c679336`; required PR checks and merge-triggered Windows/macOS/Ubuntu CI passed. Under Windows PowerShell 5.1, 133 tests initially passed and the two staging tests failed on clean `origin/main`; Task 11 fixed their path-length-sensitive fixtures. Non-elevated CLI help and auto-status pass. No protected task, service, or production state was changed.

### Task 11 - Keep deployment staging regressions within Windows PowerShell path limits
- Dependencies: Task 10 cross-version suite comparison identified two staging tests that fail on unmodified `origin/main` under Windows PowerShell 5.1 but pass under PowerShell 7.
- Files: `ops/production/windows/tests/Production.AutoDeploy.Tests.ps1`.
- Symbols: `stages trusted versioned tools before switching the poller action` and `cleans a partial tools stage and preserves the task action when copying fails`.
- Inspection: Both failures originate from excessively long synthetic test paths under Pester's Windows temporary root. Windows PowerShell 5.1 cannot copy the synthetic source module from the nested worktree path; cleanup then wraps the original copy error in an `AggregateException`. The product's scheduled task uses PowerShell 7, and these two tests already pass there.
- Required skills: `write-jane-street-style-code` and test-driven development for test-fixture repair.
- Behavior: Keep both staging tests deterministic and runnable under Windows PowerShell 5.1 and PowerShell 7 by shortening their unique per-test temporary root without weakening the staging/copy/cleanup assertions.
- Invariants: Test-only change. Preserve per-test isolation, the real source-to-stage copy path, partial-stage cleanup proof, and task-action preservation assertions.
- Boundary/API: Pester fixture paths only; no production deployment path, API, ACL, or task behavior change.
- Effects and failures: Prevent host path-length limits from masking the staging behavior under test and preserve the original simulated copy failure as the reported error.
- Tests and evidence: Reproduce both failures on clean `origin/main` with Windows PowerShell 5.1/Pester 5.9, then require both to pass under Windows PowerShell 5.1 and PowerShell 7; rerun the three related Pester suites.
- Verification: The two staging tests failed on clean `origin/main` under Windows PowerShell 5.1/Pester 5.9, then passed after shortening their unique Pester temporary roots; both also pass under PowerShell 7. The complete automatic-deploy, operations, and command suites now pass under Windows PowerShell 5.1/Pester 5.9 (135/135) and PowerShell 7/Pester 5.9 (135/135). This later fixture correction supersedes the baseline compatibility failures recorded in Tasks 9 and 10. PR #1409 merged as `62f9b92ca744b0de4a6c36e1e59a68f20f4ffc51`; required PR checks, the rerun of the transient Windows integration failure, post-merge Windows/macOS/Ubuntu CI, and post-merge CodeQL passed. No production code or state changed.

### Task 12 - Surface tool-refresh failures when protected state cannot record them
- Dependencies: Task 7 automatic tool refresh and Task 6 operator status/failure reporting.
- Files: `ops/production/windows/modules/Production.AutoDeploy.psm1` and `ops/production/windows/tests/Production.AutoDeploy.Tests.ps1`.
- Symbols: `Start-AutoDeployLoop`, `toolRefreshStatus`, and the one-shot scheduled task result.
- Inspection: When `Update-AutoDeployToolsFromOriginMain` fails, `Start-AutoDeployLoop` tries to persist `toolRefreshStatus=FAILED`, but an inner empty catch discards a second failure to read/write protected state. It then reloads the old state and proceeds to `Invoke-AutoDeployOnce`, so `auto-status` may retain a prior successful refresh and Task Scheduler may record a successful run. The old trusted tools can remain on disk; the defect is that refresh failure becomes invisible when its state record also cannot be persisted.
- Required skills: `write-jane-street-style-code` and test-driven development for the regression and fix.
- Behavior: If tool refresh fails and the failure state cannot be persisted, stop that one-shot before deployment, propagate both failures as an aggregate, publish best-effort `CHECK_FAILED` status from the in-memory failed state, and let the scheduled task record failure. Preserve normal behavior when the failure status is successfully persisted and old tools remain valid.
- Invariants: Never delete or replace the currently trusted tool bundle on failure; do not deploy using stale state after losing the durable refresh-failure record; keep status publishing best-effort; preserve both causal exceptions.
- Boundary/API: Internal SYSTEM scheduled-task behavior and existing local operator status document only; no public site route, status schema, task action, or ACL changes.
- Effects and failures: A refresh-plus-persistence failure becomes visible and retryable on the next scheduled tick while the active tool bundle and production service stay untouched.
- Tests and evidence: Add a Pester regression that makes tool refresh and protected state persistence fail, first requires the current silent continuation to fail, then asserts no deployment attempt, surfaced `CHECK_FAILED` status using the failed in-memory state, and preservation of both underlying errors. Run the automatic-deploy, operations, and command suites under PowerShell 7 and Windows PowerShell 5.1, plus CLI smoke checks.
- Verification: The new regression first failed on the old behavior because the poller continued to deployment after the state write failed. After the fix, both focused regressions pass; the automatic-deploy, operations, and command suites pass 137/137 under PowerShell 7/Pester 5.9 and Windows PowerShell 5.1/Pester 5.9. Standard-user CLI smoke checks and `git diff --check` pass. PR #1410 passed all required PR checks and merged as `706c11554048db3f042926f9407997e9e8595873`; post-merge CodeQL and CI Build passed on Windows, macOS, and Ubuntu. Production has not been changed: `auto-status` still reports `STORE_NOT_INITIALIZED`, which requires the one-time elevated `prod.cmd auto-install` bootstrap before the SYSTEM poller can take over.

### Task 13 - Publish terminal status when protected configuration preconditions fail
- Dependencies: Task 6 automatic-deploy operator status and Task 12 failure visibility.
- Files: `ops/production/windows/modules/Production.AutoDeploy.psm1`, `ops/production/windows/tests/Production.AutoDeploy.Tests.ps1`, and `docs/operations/windows-production.md`.
- Symbols: `Start-AutoDeployLoop`, `boundaryValidated`, and `CHECKING` / `CHECK_FAILED` status outcomes.
- Inspection: `Start-AutoDeployLoop` initializes the static status store and publishes `CHECKING` before loading protected deployment configuration. If `Read-ProductionConfig` or `Assert-ProductionFixedRootBoundary` fails, the outer catch sees `boundaryValidated=false` and rethrows without publishing a terminal status. The scheduled task fails, but the operator status remains `CHECKING` until it becomes stale, hiding the precondition failure.
- Required skills: `write-jane-street-style-code` and test-driven development for the regression and fix.
- Behavior: On protected configuration or fixed-root boundary failure, best-effort publish `CHECK_FAILED` with `PROTECTED_PRECONDITION` using only the already-established static status store and in-memory state, then rethrow the original error. Preserve no deployment, remote Git access, or protected-path logging before the configuration boundary is validated.
- Invariants: Do not read paths from invalid configuration, mutate deployment state, expose exception contents in public status, or replace the causal precondition exception if status publication fails. Preserve existing best-effort status behavior when the status store itself cannot be used.
- Boundary/API: Existing local operator status only; no public API/schema, ACL, deployment task, or site route changes.
- Effects and failures: A failed poll becomes visibly terminal rather than leaving a misleading fresh `CHECKING` marker; status write failure remains best-effort and the scheduled task retains the original failure result.
- Tests and evidence: Add a Pester regression for config-read and fixed-root validation failure that first proves the old path leaves `CHECKING`, then asserts `CHECK_FAILED`, preserved original error, and zero remote/deployment calls. Run focused and related automatic-deploy, operations, and command suites under PowerShell 7 and Windows PowerShell 5.1, plus non-elevated CLI smoke.
- Verification: The three new regressions failed on the baseline and passed after the fix. The automatic-deploy, operations, and command Pester suites passed 140/140 under both PowerShell 7/Pester 5.9 and Windows PowerShell 5.1/Pester 5.9; non-elevated `prod.cmd help` and `prod.cmd auto-status` passed. PR #1411 passed dependency review, CodeQL, and Windows/macOS/Ubuntu CI, then squash-merged as `89d909f3a0dd9319b0f6c6b669a21a7450faed69`; post-merge CodeQL and CI passed on all three platforms. Production acceptance remains pending because `prod.cmd auto-status` reports `STORE_NOT_INITIALIZED`; the one-time elevated `prod.cmd auto-install` bootstrap was not run. No public display-name changes are included in this release.

### Task 14 - Surface candidate output and cleanup failures
- Dependencies: Task 13 status reporting fix is merged; current checkout is clean at `b5db92681a5b8bd3b1efd5549eafc1587504c9ec`, matching refreshed `origin/main`.
- Files: `ops/production/windows/modules/Production.Deploy.psm1`, `ops/production/windows/tests/Production.Deploy.Tests.ps1`, and `docs/operations/windows-production.md`.
- Symbols: `ChristopherBell.Production.BoundedProcessLog.WriteLine`, `Start-ProductionJar`, and `Test-CandidateRelease`.
- Inspection: Read the clean merged checkout's Windows agent instructions, root README, production runbook, bounded writer, candidate startup/validation/cleanup, and Pester coverage for failed candidate startup. The writer currently catches and discards every append exception and has no capture-health property; candidate validation errors still report the path as if output was captured. Async event callbacks must remain nonthrowing.
- Required skill: `write-jane-street-style-code` before code changes.
- Behavior: Record thread-safe output-capture failure and truncation signals. After candidate process cleanup drains output callbacks, include a sanitized capture-failure or 64 KiB truncation indicator in candidate-validation errors so operators can distinguish a complete log from a missing or incomplete one. Fail candidate validation if its process remains alive after the bounded forced-stop wait.
- Invariants: Never throw from asynchronous process-output callbacks; preserve the 64 KiB bound, output ordering/serialization, and original validation failure; do not allow a release to proceed while the candidate process may remain alive; retain diagnostic logs if process output did not drain; do not include exception text, secrets, environment values, or database identifiers in diagnostics; successful candidate cleanup still removes temporary logs. Keep registered-worktree ownership and external reparse-target protections intact while making their Pester assertions portable across supported shells.
- Boundary/API: Internal production PowerShell/C# helper and local operator diagnostics only; no public route, schema, ACL, service, or deployment authorization changes.
- Effects and failures: Candidate output continues writing asynchronously to its protected log. File I/O errors change only an in-memory health flag and the later local validation message; capture failure must not interrupt process draining or replace the causal candidate failure.
- Tests and evidence: First add Pester regressions that cause the real bounded writer to append to an invalid destination, wait for event draining, and prove the old writer silently reports no failure; the existing over-64-KiB case must assert the truncation signal. Candidate-validation tests must prove failure/truncation is reported after cleanup, a still-running process blocks success and retains logs, the original validation exception remains causal, and sensitive details stay absent. Correct the four pre-existing PowerShell 5.1 failures in this deploy-test set by checking aggregate inner causes and using shell-portable junction cleanup without weakening ownership or external-target assertions. Preserve success, concurrent-write serialization, and byte-bound coverage. Run focused deploy Pester tests and related deploy/operations/command suites under PowerShell 7 and Windows PowerShell 5.1/Pester 5.9; run non-elevated CLI smoke and `git diff --check`.
- Verification: The real bounded-writer failure and candidate diagnostic regressions failed before the implementation; the surviving-process cleanup regression also failed before the fix. Focused Pester tests passed, and the deploy, auto-deploy, operations, and command suites passed 233/233 under both PowerShell 7/Pester 5.9 and Windows PowerShell 5.1/Pester 5.9. Non-elevated CLI help/status and `git diff --check` passed. PR #1412 merged as `b5db92681a5b8bd3b1efd5549eafc1587504c9ec`; required PR checks and post-merge CI Build and CodeQL passed. Production remains unchanged and standard-user `auto-status` remains `STORE_NOT_INITIALIZED`; production installation is unrelated to validating this observability behavior and still requires the one-time elevated bootstrap.

### Task 15 - Preserve the working poller bundle during bootstrap refresh
- Dependencies: Task 14 is merged as `b5db92681a5b8bd3b1efd5549eafc1587504c9ec`.
- Files: `ops/production/windows/modules/Production.AutoDeploy.psm1`, `ops/production/windows/tests/Production.AutoDeploy.Tests.ps1`, and the Windows production operations guide if recovery steps change.
- Symbols: `Update-ProductionAutoDeployToolsUnderHeldLock`, `Install-AutoDeployTask`, and the `ChristopherBellAutoDeploy` scheduled task.
- Inspection: On current merged `origin/main`, `Update-ProductionAutoDeployToolsUnderHeldLock` stops the scheduled task, recursively deletes the active `tools` directory, creates the replacement at the live path, then copies and validates files before re-registering the task. A copy, ACL, integrity, or registration failure after deletion can leave the task action pointing at missing or partial tools and removes the last known-good bundle. The repeating trigger is not disabled around this replacement, so another scheduled invocation can race the stop/check and filesystem switch.
- Required skills: `write-jane-street-style-code` and test-driven development for the failure regression.
- Behavior: Copy and fully protect/validate a sibling staged tool bundle before changing the active bundle. Prevent new scheduled starts while switching. Replace the active bundle and task action only after staging succeeds; if switching or registration fails, restore the prior bundle and task registration before returning the original failure. Explicitly re-enable and start the task only after the new bundle and action are valid.
- Invariants: Never delete the last known-good tool bundle before a validated replacement exists; never allow the scheduled task to execute from a partial bundle; preserve the prior task action and enabled state on pre-switch staging failure; retain protected ACL, fixed-root, no-reparse, trusted-source, and SYSTEM identity boundaries; preserve causal errors if rollback also fails.
- Boundary/API: Protected Windows deployment tools and scheduled-task lifecycle only; no public site route, schema, service identity, deploy authorization, or production data changes.
- Effects and failures: A failed stage leaves the existing task and bundle usable. A failed cutover restores the last known-good tools and scheduled task when possible and returns both the primary and recovery failures when rollback cannot complete. A successful install starts the refreshed poller once after publishing the validated bundle.
- Tests and evidence: First add a Pester regression that forces bundle copy/validation to fail and proves the old bundle and scheduled-task action remain intact; demonstrate the current implementation loses the old bundle. Cover trigger disable/stop ordering, successful replacement/re-enable/start ordering, and rollback when task registration fails. Run focused and related auto-deploy, operations, and command suites under PowerShell 7 and Windows PowerShell 5.1/Pester 5.9, plus non-elevated CLI smoke and `git diff --check`. Do not invoke the elevated installer or mutate production while validating locally.
- Verification: The staging, task-disable ordering, registration rollback, first-install cleanup, and recovery-error regressions failed before implementation and passed after the fix. Deploy, automatic-deploy, operations, and command suites passed 237/237 under PowerShell 7/Pester 5.9 and Windows PowerShell 5.1/Pester 5.9. Non-elevated CLI help/status and `git diff --check` passed. PR #1413 merged as `a4c03e8040c772f64e527ed96b601c45d03eadaf`; required PR checks and post-merge CI Build and CodeQL passed. Production was not modified; standard-user auto-status remains `STORE_NOT_INITIALIZED` and bootstrap still requires the documented elevated install.

### Task 16 - Make automatic-deploy task removal observable and verifiable
- Dependencies: Task 15 is merged as `a4c03e8040c772f64e527ed96b601c45d03eadaf`.
- Files: `ops/production/windows/modules/Production.AutoDeploy.psm1` and `ops/production/windows/tests/Production.AutoDeploy.Tests.ps1`; update `docs/operations/windows-production.md` only if the documented command outcome or recovery behavior changes.
- Symbols: `Remove-AutoDeployTask`, its lock-held removal helper, `Get-ProductionAutoDeployTask`, and the `ChristopherBellAutoDeploy` scheduled task.
- Inspection: On merged `origin/main`, `Remove-AutoDeployTask` suppresses all errors from `Stop-ScheduledTask` and `Unregister-ScheduledTask`, does not check whether the task exists or is actually removed, and returns no result that distinguishes success from failure. The CLI invokes this function directly with `$ErrorActionPreference = 'Stop'`, so explicitly terminating scheduler errors will reach the operator. Further review confirmed automatic deployment and poller installation use the fixed-root deployment lock while task removal bypasses it; removal can therefore stop a poller during an active deployment or race tool/task refresh.
- Required skills: `write-jane-street-style-code` and test-driven development for the failure regressions.
- Behavior: Acquire the same fixed-root deployment lock used by deployment and poller installation before inspecting or changing the task, and always release it. Make removal idempotent when the task is absent. When present, disable it and verify the disabled state before stopping a running task; unregister it and verify it is absent before reporting success. Surface scheduler failures and failed postconditions. If cleanup fails after changing the task's enabled state, attempt to restore and verify the prior enabled state, and preserve both the primary and recovery errors.
- Invariants: Never claim removal succeeded while the task remains registered; do not interrupt a deployment or race poller installation/refresh; do not hide lock, access-denied, scheduler, stop, unregister, or verification failures; do not alter unrelated scheduled tasks; preserve administrator and WhatIf boundaries; retain the original failure if recovery also fails.
- Boundary/API: Local Windows scheduled-task lifecycle only; no website route, database, service identity, release, or production deployment changes.
- Effects and failures: Successful removal leaves no root `ChristopherBellAutoDeploy` task. An absent task is a successful no-op. Failed lock acquisition causes no scheduler operations. Failed removal returns an actionable failure and restores the task's previous enabled/disabled state when recovery is possible; the shared deployment lock is released on success and failure.
- Tests and evidence: Add Pester regressions showing stop and unregister failures are currently swallowed and a no-op unregister is misreported as success. Cover lock acquisition before task lookup/mutation, lock release on success and failure, no scheduler calls on lock failure, absent task, disabled/running tasks, disable-stop-unregister ordering, verification of task disable and absence, restoration after unregister failure, and surfaced restoration failures. Run focused and related automatic-deploy, operations, and command suites under PowerShell 7 and Windows PowerShell 5.1/Pester 5.9, non-elevated CLI smoke, and `git diff --check`. Do not invoke `auto-remove` against production.
- Verification: The removal regressions failed on the baseline; lock-acquisition, lock-before-task-lookup, lock-release, idempotence, stop/unregister failure, no-op unregister, state restoration, and recovery-error regressions pass after implementation. Automatic-deploy, operations, and command suites passed 154/154 under PowerShell 7/Pester 5.9 and Windows PowerShell 5.1/Pester 5.9. Non-elevated `prod.cmd help` and `prod.cmd auto-status` passed; status correctly reported `STORE_NOT_INITIALIZED`. `git diff --check` passed. PR #1414 checks passed and it merged as `b5de55a1c1225d48f4c278b4b3706ef02a091387`; post-merge CI Build and CodeQL passed on that SHA. The removal command was not run against production.

### Task 17 - Make production service uninstall fail closed and verifiable
- Dependencies: Independent of Task 16. The implementation branch was brought up to date with Task 16 merge `b5de55a1c1225d48f4c278b4b3706ef02a091387` before final verification.
- Files: `ops/production/windows/modules/Production.Install.psm1`, `ops/production/windows/tests/Production.Install.Tests.ps1`, and the Windows production operations guide only if command recovery guidance changes.
- Symbols: `Uninstall-ProductionRuntime`, `ChristopherBellDev`, and the WinSW service uninstall command.
- Inspection: On merged `origin/main`, `Uninstall-ProductionRuntime` suppresses errors when querying and stopping `ChristopherBellDev`, then invokes WinSW and checks only its exit code. Query failure can be treated as absence, stop failure is hidden, and exit code zero is trusted without verifying the service is gone. No direct uninstall tests currently cover these cases.
- Required skills: `write-jane-street-style-code` and test-driven development for the failure regressions.
- Behavior: Keep uninstall idempotent when the service is genuinely absent. When present, stop it with surfaced errors, verify it reaches `Stopped`, invoke the fixed WinSW binary and capture its exit result immediately, then verify the service is absent before reporting success. Report an actionable failure when the binary is unavailable, WinSW fails, or the service remains registered.
- Invariants: Never report successful uninstall while the service remains registered or stop state is unknown; never treat service-query errors as absence; preserve the existing administrator and WhatIf boundaries; do not remove MongoDB, cloudflared, application data, backups, secrets, or releases.
- Boundary/API: Local native Windows service lifecycle only; no website route, database, service identity, release, or production deployment changes.
- Effects and failures: Successful uninstall removes only the `ChristopherBellDev` service. An absent service is a successful no-op. Any query, stop, timeout, WinSW, or postcondition failure reaches the operator and prevents a success result.
- Tests and evidence: Add Pester regressions showing suppressed query/stop errors and a successful no-op WinSW command are currently misreported. Cover absent service, running service stop and wait ordering, stop failure, executable absence, nonzero WinSW exit, service still registered after zero exit, and WhatIf without side effects. Run focused and related install, operations, and command suites under PowerShell 7 and Windows PowerShell 5.1/Pester 5.9, non-elevated CLI smoke, and `git diff --check`. Do not invoke uninstall against production.
- Verification: The new service-query and service-stop regressions failed before implementation. All nine focused uninstall regressions passed, including exact WinSW exit-code capture and rejection when the service remains registered after a zero exit. Install, operations, and command suites passed 211 tests with one existing skip under PowerShell 7/Pester 5.9 and Windows PowerShell 5.1/Pester 5.9. Non-elevated `prod.cmd help` and `prod.cmd auto-status` passed; status correctly reported `STORE_NOT_INITIALIZED`. `git diff --check` passed. PR #1415 required checks passed and it merged as `15288bebf60e7e330956ae3b8aaf6f2d22f3770b`; post-merge CI Build and CodeQL passed on that SHA. Uninstall was not invoked against production.

### Task 18 - Keep production status from hiding service and listener query failures
- Dependencies: Task 17 is merged as `15288bebf60e7e330956ae3b8aaf6f2d22f3770b`; start from refreshed `origin/main`.
- Files: `ops/production/windows/modules/Production.Operations.psm1` and `ops/production/windows/tests/Production.Operations.Tests.ps1`.
- Symbols: `Get-ProductionStatus`, the website/MongoDB/cloudflared service queries, and the production listener query.
- Inspection: On current `origin/main`, `Get-ProductionStatus` uses `-ErrorAction SilentlyContinue` for all three service lookups and `Get-NetTCPConnection`; any query failure is represented as `NotInstalled` or a null `ProductionPortPid`, indistinguishable from a genuine absent service/no listener. Its returned object therefore can look like a valid status snapshot when local management queries failed.
- Required skills: `write-jane-street-style-code` and test-driven development for the failure regressions.
- Behavior: Preserve `NotInstalled` for genuinely absent services and a null PID for a successful listener query with no results. Propagate non-absence service-query errors and listener-query failures to the caller rather than returning a misleading snapshot. Keep the status object's existing fields and successful values unchanged.
- Invariants: Do not turn a management-query failure into a successful-looking status; do not modify services, listeners, release pointers, or the status schema; preserve existing administrator and protected-config boundaries.
- Boundary/API: Local production CLI diagnostics only; no public route, service lifecycle, database, or production mutation changes.
- Effects and failures: A complete query returns the existing status object. A genuine missing service stays `NotInstalled`; inability to inspect services or the listener causes a visible command failure with the causal error.
- Tests and evidence: Add Pester regressions showing the current code suppresses service-query and listener-query errors, plus successful, missing-service, and no-listener behavior. Run focused and related operations, command, and automatic-deploy suites under PowerShell 7 and Windows PowerShell 5.1/Pester 5.9, non-elevated CLI smoke, and `git diff --check`. Do not invoke mutating production commands.
- Verification: The service-query failure, listener-query failure, native missing-service behavior, empty-listener behavior, and successful listener PID regressions pass; the prior implementation swallowed query failures and a listener-query failure surfaced as an unrelated strict-mode property error. Operations, command, and automatic-deploy Pester suites passed 158/158 under PowerShell 7/Pester 5.9 and Windows PowerShell 5.1/Pester 5.9. Non-elevated `prod.cmd help` passed; `prod.cmd auto-status` completed and correctly reported `STORE_NOT_INITIALIZED`. The native missing-service error identifier was confirmed identical in both shells. `git diff --check` passed. PR #1416 passed dependency review, CodeQL, and Windows/macOS/Ubuntu checks, then squash-merged as `d03a34c2d666c34615c8b20f340a1e0d0c801067`; post-merge CI Build and CodeQL passed. No mutating production command was invoked. Production bootstrap and acceptance remain pending because the automatic-deploy store is uninitialized and require the user's elevated approval; no prompt was opened.

### Task 19 - Surface release-retention enumeration failures
- Dependencies: Task 18 is merged as `d03a34c2d666c34615c8b20f340a1e0d0c801067`; begin from refreshed `origin/main`.
- Files: `ops/production/windows/modules/Production.Deploy.psm1` and `ops/production/windows/tests/Production.Deploy.Tests.ps1`.
- Symbols: `Remove-ExpiredReleases`, the releases directory enumeration, and protected current/previous/schema-direction release retention.
- Inspection: `Remove-ExpiredReleases` uses `Get-ChildItem -ErrorAction SilentlyContinue`; an access, I/O, or provider error becomes an empty release list. Deployment then continues as though retention succeeded, potentially leaving unbounded stale releases and consuming the volume over repeated deploys. The release directory is established before retention runs; protected-release filtering and individual removal errors already fail visibly.
- Required skills: `write-jane-street-style-code` and test-driven development for the failure regression.
- Behavior: Surface release-directory enumeration failures to the deployment caller instead of treating them as an empty directory. Preserve normal ordering, retention count, schema-direction protections, and successful empty-directory behavior only if the directory is genuinely absent for a supported lifecycle state.
- Invariants: Never report retention as completed after an enumeration failure; never remove current, previous, marker-owned, or within-retention releases; preserve path-boundary checks and causal exceptions. No production cleanup is invoked during local verification.
- Boundary/API: Internal deployment cleanup only; no CLI, status schema, service, release pointer, database, or public route changes.
- Effects and failures: On successful enumeration, existing retention cleanup remains unchanged. Enumeration access/I/O failures abort cleanup and propagate so the caller can report the deploy failure; already-switched release state and rollback behavior remain governed by the existing deployment transaction.
- Tests and evidence: Add a regression that makes release enumeration fail and confirms the old implementation suppresses it, then assert the failure propagates before any stale release removal. Preserve the existing schema-direction retention test. Run focused deploy Pester tests plus related deploy, automatic-deploy, operations, and command suites under PowerShell 7 and Windows PowerShell 5.1/Pester 5.9, non-elevated CLI smoke, and `git diff --check`. Do not invoke mutating production commands.
- Verification: The new failure regression failed before implementation because `Remove-ExpiredReleases` returned normally after the mocked release enumeration error; it now propagates the error before removal. Deploy, automatic-deploy, operations, and command suites passed 252/252 under PowerShell 7/Pester 5.9 and Windows PowerShell 5.1/Pester 5.9. Non-elevated `prod.cmd help` passed; `prod.cmd auto-status` reported `STORE_NOT_INITIALIZED`. `git diff --check` passed. A checkout-generated line-ending-only `gradlew.bat` change was left untouched and excluded. PR #1417 passed dependency review, CodeQL, and Windows/macOS/Ubuntu checks, then squash-merged as `1478029f872d29a27c7a31a37300264dfc2c8544`; post-merge CI Build and CodeQL passed. No production command that mutates state was invoked.

### Task 20 - Surface production log and release listing query failures
- Dependencies: Task 19 is merged as `1478029f872d29a27c7a31a37300264dfc2c8544`; begin from refreshed `origin/main`.
- Files: `ops/production/windows/modules/Production.Operations.psm1` and `ops/production/windows/tests/Production.Operations.Tests.ps1`.
- Symbols: `Watch-ProductionLogs`, `Get-ProductionReleases`, and their `Get-ChildItem` calls.
- Inspection: Both operator commands use `Get-ChildItem -ErrorAction SilentlyContinue`. An access or I/O error while reading the initialized protected `logs` or `releases` directories is presented as “No production log file exists” or an empty release list. The install-root initializer creates both directories; current `Get-ProductionStatus` already demonstrates the corrected policy for other management queries. The CLI maps `logs` and `releases` directly to these functions.
- Required skills: `write-jane-street-style-code` and test-driven development for the failure regressions.
- Behavior: Propagate unexpected directory-enumeration failures to the operator. Preserve the existing custom no-log result for a successful empty log-directory query and the empty release-list result for a successful empty releases-directory query.
- Invariants: Do not hide an unreadable or failed management query as valid empty output; do not alter log rotation, release ordering/current/previous flags, or retention; preserve protected-config and administrator boundaries. No production logs or releases are modified during local verification.
- Boundary/API: Local read-only production CLI diagnostics; no status schema, service, release pointer, database, or public route changes.
- Effects and failures: Successful enumeration keeps existing output. Access/I/O errors become visible command failures with the causal query error.
- Tests and evidence: Add regressions for log and release enumeration errors, successful empty-directory behavior, and existing release ordering/current/previous values as needed. Run focused operations Pester tests plus related operations, command, and automatic-deploy suites under PowerShell 7 and Windows PowerShell 5.1/Pester 5.9; run non-elevated CLI smoke and `git diff --check`. Do not invoke mutating production commands.
- Verification: New regressions confirmed that log-query errors were converted to "No production log file exists" and release-query errors were converted to empty output; both now propagate. Added successful empty-directory assertions and a release ordering/current/previous regression. Operations, command, and automatic-deploy suites passed 163/163 under PowerShell 7/Pester 5.9 and Windows PowerShell 5.1/Pester 5.9. Non-elevated `prod.cmd help` passed; `prod.cmd auto-status` reported `STORE_NOT_INITIALIZED`. `git diff --check` passed. No production mutation was performed. PR #1418 passed all required PR checks and merged as `3260fe04319914ec3049059906d90e7b683e3d40`; post-merge CI Build and CodeQL passed on that SHA.

### Task 21 - Distinguish cloudflared service absence from query failure
- Dependencies: Task 20 is merged as `3260fe04319914ec3049059906d90e7b683e3d40`; begin from refreshed `origin/main`.
- Files: `ops/production/windows/modules/Production.Install.psm1` and `ops/production/windows/tests/Production.Install.Tests.ps1`.
- Symbols: `Install-CloudflaredService` and its `Get-Service cloudflared` query.
- Inspection: The installer uses `Get-Service cloudflared -ErrorAction SilentlyContinue` and treats every query error as an absent service. On query failure it can issue a misleading missing-token error or proceed into service installation without knowing whether the service already exists. Native PowerShell reports a genuinely missing service as `NoServiceFoundForGivenName,Microsoft.PowerShell.Commands.GetServiceCommand`; unexpected query failures must remain visible.
- Required skills: `write-jane-street-style-code` and test-driven development for the failure regression.
- Behavior: Treat only the native missing-service result as absence. Propagate all other service-query failures before token validation, service installation, or service state changes. Preserve existing behavior for an absent service, an existing service, token replacement, and WhatIf.
- Invariants: Never interpret access/query errors as absence; do not install or uninstall cloudflared after an unexpected query failure; preserve signed-binary, protected-token, service-binding, administrator, and WhatIf boundaries; do not expose token contents.
- Boundary/API: Local cloudflared service installation diagnostics only; no public routes, website service, database, scheduled task, or production deployment changes.
- Effects and failures: A genuinely absent service still requires a protected token path. A present service without a replacement token is still validated against the configured signed executable. Unexpected query errors reach the operator with their original cause and cause no downstream process/service operations.
- Tests and evidence: Add a regression showing an unexpected `Get-Service` error is currently mistaken for absence and assert it propagates before `Invoke-CheckedProcess`, `Set-Service`, or `Start-Service`. Cover the native missing-service error and existing-service paths to preserve their behavior. Run focused install tests and related operations/command suites under PowerShell 7 and Windows PowerShell 5.1/Pester 5.9, non-elevated CLI smoke, and `git diff --check`. Do not invoke the elevated installer or mutate production.
- Verification: The unexpected service-query regression failed before the fix because `SilentlyContinue` suppressed the mocked access error; after the fix the error propagates before any service or process action. The missing-service token requirement is now exercised with the native `NoServiceFoundForGivenName,Microsoft.PowerShell.Commands.GetServiceCommand` identifier. Install, operations, and command suites passed 221 tests with one existing skip under PowerShell 7/Pester 5.9 and Windows PowerShell 5.1/Pester 5.9. Non-elevated `prod.cmd help` passed; `prod.cmd auto-status` reported `STORE_NOT_INITIALIZED`; `git diff --check` passed. PR #1419 passed all required checks and merged as `f506b6e7aa69a5083dbd0f8da2d79387d4da2367`; post-merge CI Build passed on Windows, macOS, and Ubuntu, and CodeQL passed. No production mutation was performed.

### Task 22 - Make automatic-deploy task refresh wait safely for transient lock contention
- Dependencies: Task 21 is merged as `f506b6e7aa69a5083dbd0f8da2d79387d4da2367`; begin from refreshed `origin/main`.
- Files: `ops/production/windows/modules/Production.Common.psm1`, `ops/production/windows/modules/Production.AutoDeploy.psm1`, and their focused Pester tests. Update the Windows production guide only if the operator-visible wait or timeout behavior needs documentation.
- Symbols: `Enter-DeploymentLock`, `Install-AutoDeployTask`, `ChristopherBellAutoDeploy`, and the shared protected `locks/deploy.lock`.
- Inspection: The task repeats every `autoDeployPollSeconds` (60 seconds in the configured production instance) and uses the same exclusive deployment lock as manual task installation. `Install-AutoDeployTask` currently fails immediately when the poller briefly owns that lock. The user reported `prod.cmd auto-install` failing with "Another production operation is already running." An elevated read-only check found the SYSTEM task enabled and Ready with `LastTaskResult=0`; the lock file metadata is old and does not identify its handle owner. The user is away, so do not retry elevated installation or alter the task/lock.
- Required skills: `write-jane-street-style-code` and test-driven development for lock contention and timeout behavior.
- Behavior: Allow `Install-AutoDeployTask` to wait for a bounded period when the lock is held by an actual Windows sharing/lock violation, then proceed when the lock is released. Keep all other lock users fail-fast by default. On timeout, fail with an actionable busy message and make no task or tool-tree changes.
- Invariants: Never remove, steal, truncate, or bypass the lock; never retry access, path, ACL, or unrelated I/O errors as contention; preserve fixed-root, reparse, administrator, staging/rollback, and scheduled-trigger safeguards; dispose acquired lock handles on all paths.
- Boundary/API: Local automatic-deploy task refresh locking only; no public routes, database, website service, production configuration, or live scheduled-task changes during verification.
- Effects and failures: A brief poller lock no longer causes an immediate manual installer failure. A real long-running deployment still blocks refresh until it releases the lock or the bounded wait expires. Timeout and non-contention I/O failures remain visible, and scheduler/tree state is unchanged on either failure.
- Tests and evidence: Add a regression proving transient lock contention currently makes `Install-AutoDeployTask` fail immediately; cover successful acquisition after release, bounded timeout, non-contention I/O failure without retry, default fail-fast behavior for other lock callers, and no task/tree changes before acquisition. Run focused common/automatic-deploy tests plus related operations/command suites under PowerShell 7 and Windows PowerShell 5.1/Pester 5.9, non-elevated CLI smoke, and `git diff --check`. Do not invoke `auto-install` or mutate production.
- Verification: The lock-wait and installer-contract regressions failed before implementation because `Enter-DeploymentLock` lacked bounded waiting and `Install-AutoDeployTask` passed no wait timeout. After implementation, transient sharing/lock violations are retried only when an explicit timeout is supplied; the default remains fail-fast, unrelated I/O errors are propagated without retry, and an expired wait gives an actionable busy error. The four regression cases passed. Common, automatic-deploy, operations, and command suites passed 195/195 under PowerShell 7/Pester 5.9 and Windows PowerShell 5.1/Pester 5.9. `prod.cmd help` passed; non-elevated `prod.cmd auto-status` reported `STORE_NOT_INITIALIZED`; `git diff --check` passed. PR #1420 passed dependency review, CodeQL, and Windows/macOS/Ubuntu CI, then merged as `76bd6ad450c306ee7aa22db84df9c93e242821fd`; post-merge CI Build passed on all three platforms and CodeQL passed. No live auto-install or production mutation was performed while the user was away.

### Task 23 - Surface PostgreSQL service query failures in PostgreSQL status
- Dependencies: Task 22 is merged as `76bd6ad450c306ee7aa22db84df9c93e242821fd`; start from refreshed `origin/main`.
- Files: `ops/production/windows/modules/Production.PostgreSql.psm1` and `ops/production/windows/tests/Production.PostgreSql.Tests.ps1`.
- Symbols: `Get-ProductionPostgreSqlStatus` and its `Get-Service -Name $Config.postgresqlServiceName` query.
- Inspection: The status probe validates PostgreSQL's database identity and settings, then uses `Get-Service -ErrorAction SilentlyContinue` to populate its `Service` field. Every query failure is silently converted to `Service='NotInstalled'`; an access or service-control query failure can therefore return a misleading healthy database status that claims the service is absent. Other service status queries now surface unexpected failures, and the cloudflared installer established the native missing-service `FullyQualifiedErrorId` handling pattern.
- Required skills: `write-jane-street-style-code` and test-driven development for query-failure and genuine-absence behavior.
- Behavior: Preserve `Service='NotInstalled'` only for the native Get-Service missing-service result. Propagate unexpected service-query failures after the existing database probe, preserving their cause and existing status schema.
- Invariants: Never interpret access/provider/I/O errors as service absence; do not expose secrets; do not change the database probe, service lifecycle, PostgreSQL configuration, or production state.
- Boundary/API: PostgreSQL status diagnostics only; no schema, CLI shape, database, service, migration, or public route changes.
- Effects and failures: A successfully queried installed service retains its observed state. A genuinely absent service remains `NotInstalled`. An unexpected service query error fails the status operation rather than returning incomplete or misleading service information.
- Tests and evidence: Add a regression that reproduces suppression of an unexpected `Get-Service` error and proves it propagates; preserve successful service and native-missing-service status behavior. Run focused PostgreSQL Pester tests plus related operations and command suites under PowerShell 7 and Windows PowerShell 5.1/Pester 5.9, non-elevated CLI smoke as practical, and `git diff --check`. Do not invoke a live PostgreSQL mutation or elevated command.
- Verification: The unexpected `Get-Service` regression failed before the fix because `-ErrorAction SilentlyContinue` suppressed the mocked query error and returned `NotInstalled`. After the fix, an installed/running service retained its status, only the native missing-service `FullyQualifiedErrorId` returned `NotInstalled`, and unexpected query failures propagated. The targeted status cases passed 3/3 in Windows PowerShell 5.1/Pester 5.9; the PostgreSQL, operations, and command suites passed 150/150 in PowerShell 7/Pester 5.9. Under Windows PowerShell, the PostgreSQL suite had 148 passing cases and 2 unrelated existing failures because that runtime lacks `RandomNumberGenerator.Fill` and `Convert.ToHexString`; the focused service cases passed 3/3 and operations/command passed 120/120. `prod.ps1 help` and `git diff --check` passed. PR #1421 passed required PR checks and merged as `97c5a339a03cd21ce95be5e800e036940bd6f1df`; post-merge CI Build and CodeQL are in progress. No production mutation or elevation was performed.

### Task 24 - Fail safely when PostgreSQL legacy replacement inspection fails
- Dependencies: Task 23 is merged as `97c5a339a03cd21ce95be5e800e036940bd6f1df`; post-merge verification is tracked above. Start from refreshed `origin/main` at that merge.
- Files: `ops/production/windows/modules/Production.PostgreSql.psm1` and `ops/production/windows/tests/Production.PostgreSql.Tests.ps1`.
- Symbols: `Enter-ProductionPostgreSqlLegacyReplacement` and its `Get-Service`/`Get-NetTCPConnection` preflight queries.
- Inspection: Refreshed `origin/main` in `A:\Projects\christopherbell.dev-worktrees\production-query-observability-20260924` at `97c5a339a03cd21ce95be5e800e036940bd6f1df`. Read the function and its existing rollback test. Its `Get-Service -ErrorAction SilentlyContinue` treats every lookup error as an absent legacy service; its established-client `Get-NetTCPConnection -ErrorAction SilentlyContinue` treats every query error as zero clients, after which a running PostgreSQL 16 service can be disabled and stopped. This is an unsafe preflight failure mode.
- Required skill: `write-jane-street-style-code`; use test-driven development for preflight failure behavior.
- Behavior: A genuinely absent legacy service remains a no-op only when `Get-Service` returns its native missing-service error. Unexpected service-query errors and any connection-inspection error must propagate before service changes. A successful empty connection query continues to permit the existing replacement flow; any observed connection still blocks it.
- Invariants: Never stop or disable PostgreSQL 16 unless service state, service startup mode, and client-connection state were successfully inspected. Preserve current rollback behavior, outputs, service naming, and administrator boundary. Do not connect to or mutate production.
- Boundary/API: Local PostgreSQL installer preflight and rollback only; no database schema/configuration, public routes, CLI shape, or production changes.
- Effects and failures: Inspection errors fail installation before changing service startup type or state. Successful inspection retains existing active-client rejection and no-client replacement behavior. The original query error remains causal.
- Tests and evidence: Add regressions for unexpected service lookup failure, native absent-service behavior, and connection enumeration failure; assert no `Set-Service` or `Stop-Service` occurs on inspection failure. The exception precedes installer process actions in the orchestration flow. Run PostgreSQL tests and related operations/command suites under PowerShell 7 and Windows PowerShell 5.1/Pester 5.9; record the known unrelated Windows PowerShell API failures without attributing them to this change. Run non-elevated CLI help and `git diff --check`. No elevated/live installation is needed or authorized while the user is away.
- Verification: The service-query and connection-query regressions each failed against the silencing behavior: service failure was replaced by a missing `Status` property error, and connection failure was suppressed with no thrown error. After the fix, all three focused tests passed, including native missing-service handling; the tests also assert no service mutation on either inspection failure. PostgreSQL, operations, and command suites passed 153/153 in PowerShell 7/Pester 5.9. Windows PowerShell 5.1/Pester 5.9 passed 151 cases and had the same two unrelated .NET API compatibility failures (`RandomNumberGenerator.Fill`, `Convert.ToHexString`). `prod.ps1 help` and `git diff --check` passed. PR #1422 passed all required checks and merged as `6a669a3c151e3564de607362c8421acce2f7d421`; post-merge CI Build and CodeQL are running. No live production change occurred.

### Task 25 - Surface sensor inventory and listener query failures
- Dependencies: Task 24 merged as `6a669a3c151e3564de607362c8421acce2f7d421`; start from refreshed `origin/main` on `codex/sensor-query-errors-20260924`.
- Files: `ops/production/windows/modules/Production.Sensors.psm1` and `ops/production/windows/tests/Production.Sensors.Tests.ps1`.
- Symbols: `Get-PawnIoInstallation`'s `Get-CimInstance Win32_SystemDriver` query and `Get-ProductionCpuTemperature`'s production-listener query.
- Inspection: Read both functions and the sensor status/install/readiness tests in refreshed `origin/main` at `6a669a3c151e3564de607362c8421acce2f7d421`. The driver inventory suppresses all CIM failures and returns `Driver='Missing'`, indistinguishable from a successful empty inventory. CPU-temperature ownership suppresses all listener-query errors and reports a misleading listener count of zero instead of the query failure. Other sensor health checks use `-ErrorAction Stop`.
- Required skill: `write-jane-street-style-code`; use test-driven development for diagnostic failure behavior.
- Behavior: Surface unexpected driver inventory and listener query failures. Preserve `Driver='Missing'` when the CIM query succeeds with no driver, and preserve the existing listener-count validation for a successful query.
- Invariants: Do not treat an unobservable driver or listener state as absence; do not alter driver installation, service, sensor enablement, listener ownership, protected file, or production state.
- Boundary/API: Local operator sensor diagnostics only; no public site routes or production mutation.
- Effects and failures: Query failures remain causal errors to the caller; successful empty query results retain their existing missing-driver/no-listener outcomes.
- Tests and evidence: Add focused Pester regressions proving both query errors propagate and asserting sensor/production mutations are not attempted; preserve successful empty-driver and zero-listener cases. Run the sensor, operations, and command suites under PowerShell 7 and Windows PowerShell 5.1/Pester 5.9, non-elevated CLI smoke, and `git diff --check`. No elevated or live production sensor command.
- Verification: The driver-inventory regression failed before the fix because the query error was suppressed and `Get-PawnIoInstallation` returned a missing-driver result. The listener-query regression failed because the suppressed error was reported as `found 0` listeners. After the fix, all four focused failure/empty-result cases passed in PowerShell 7 and Windows PowerShell 5.1/Pester 5.9. Sensor, operations, and command suites passed 152/152 in PowerShell 7. The Windows PowerShell 5.1 full suites passed 146 cases and had 6 unrelated existing test/runtime compatibility failures: `Path.GetRelativePath`, `IO.Compression.ZipFile`, and `Double.IsFinite`. The supported `prod.cmd` launcher requires PowerShell 7, while all 4 new focused cases pass under Windows PowerShell 5.1. Non-elevated CLI help and `git diff --check` passed. PR #1423 is open; required checks are running. Task 24's post-merge CI Build remains in progress and CodeQL passed.

## Code Changes
Task 2 is limited to WFL startup catch-up selection, direct unit tests and the owning feature README's scheduling contract. Task 3 extends the existing Cane's weekly collector's startup behavior without changing its persistence schema or API. Task 8 narrows swallowed profile-resolution failures while preserving anonymous fallback behavior. Reinspect all targets before edits.

## Files and Modules
Spring services for WFL restaurant imports, Cane's weekly price collection, and public account profiles; their unit tests and owning feature READMEs.

## Unit Testing
Run the focused import workflow test, `AccountServiceTest`, and existing JavaScript, Java and Windows production checks through `:website:check`. Do not run database-backed tests without verified database `test` isolation and credentials.

## Local Testing
Start the packaged candidate on an alternate port using an ephemeral isolated PostgreSQL cluster/database named `test`, with the `deploy-smoke` profile disabling external scheduled effects. Never touch production listeners or data.

## Validation
Review the source/test/documentation diff; run `git diff --check`, the focused test, and `:website:check`. Required PR CI must pass. Complete alternate-port runtime proof and post-deployment production checks through the supported deployment procedure.

## Rollback or Recovery
Use the site's documented protected deployment rollback procedure if production acceptance fails. Do not manually stop/replace production processes or alter protected state. Record the merged revision and prior deployed artifact identity.

## Risks
The stale freshness marker confirms data age, not why scheduled imports stopped succeeding. Startup retry addresses overdue successful-completion state; a persistent upstream, database, or environment failure can still leave data stale. Isolated runtime infrastructure must not share production databases or writers.

## Completion Criteria
The reported cadence defects and public-profile viewer-state failure have regression tests that fail before and pass after their fixes; native checks and alternate-port runtime proofs pass; required CI passes and each reviewed fix merges. The automatic deployer's non-elevated status path must pass its exact path, ACL, staleness, sanitization, and failure-surfacing checks, and the installed SYSTEM task must refresh trusted deployment tools without routine prompts. Supported production deployment and exact production acceptance evidence, dated Builder session record/index validation/publication, and explicit reporting of remaining stale-data causes or verification limitations remain required. The Builder plan stays in-progress until those steps finish.
